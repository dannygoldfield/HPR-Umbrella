# HPR comparison choices — 7 September 2026

Danny reviewed the comparison in Chrome and explicitly locked these settings for the next iteration:

| Portrait | Develop | Sound bed |
| --- | --- | --- |
| To Live 10,000 Years | 150% | 25% quieter |
| NYChildren | 150% | 50% quieter |
| 1 to Infinity | 200% | 50% quieter |

The Infinity field is **not chosen**. These are comparison selections, not final component or AV loop approvals. Final application deliveries remain pending human AV review. Preserve the original selected compositions (options 1, 10 and 7 respectively), their foreground events and relationships, native eleven-second duration and three-pass, 33-second presentation. Bed percentages refer to the bed component's amplitude; do not renormalize the complete mix.

The machine-readable record is `config/radcliffe-comparison-choices-2026-09-07.json`. It identifies and fingerprints each selected native visual, native audio, source manifest and reviewed 33-second file, with the component lock at selection. Paths resolve against the indicated canonical repository. Infinity's static development preview identifies its chosen develop setting only; it does not authorize a static final field. Browser drafts are not the authority for these recorded choices.

## Infinity motion finding

Danny reported seeing no background animation in any option. This is a failed perceptual comparison even though the rendering pipeline works.

The existing number field has already been reconstructed as separate WebGL glyphs using the existing digits, coordinates, typeface, palette and subject alpha. It is composited behind the separate photograph. The static source does not freeze the animated background. The review plays pre-rendered MP4s; it does not require live WebGL in the review browser.

Inspection of each first eleven-second cycle at 270 × 480, excluding the subject and a generous edge margin, found 264 distinct background frames in every animated source background, native composite and review MP4. The static source and native composite each had one distinct background frame. Small changes in the encoded static review control are compression variation, not intended movement.

All three motion formulas are capped at a maximum displacement of three pixels from the resting position at 1080-pixel output width. Maximum displacement scales below one display pixel when the actual image is narrower than 360 pixels. Pressure and flow affect only part of the field, reducing their visibility further. These settings were too restrained for this review; the evidence does not require a background rebuild. The check confirms motion in the exact files, not what played on Danny's screen.

## Smallest useful next experiment

Keep the approved full-body photograph, current number design, photographic geometry and selected 200% development. First calibrate visibility with the deflected wave: compare a static control with six- and twelve-pixel maximum displacements at full resolution. Show one large portrait at a time, retaining the eleven-second periodic motion and three-pass presentation. Change no palette, density, crop, sound or motion timing. This is a proposed test, not a selected field or an instruction to replace an approved master.

Once a useful visibility range is established, compare wave, pressure and flow at that range. Judge whether the person feels more present, not whether viewers can identify the effect. Do not multiply all three approaches by many strengths before calibrating one. Stronger displacement can reveal the mechanism or draw attention to the silhouette, so retain the static reference. The final three-video AV review must wait for the Infinity field decision and human loop checks.

## Preservation and verification

All six chosen native media files, six source manifests and six comparison files matched their recorded fingerprints. Both canonical generator commits and the three original five-star visual fingerprints passed verification. No generator code, component lock, rendered media, Registry records or browser storage was changed by recording these choices.

Private diagnostic measurements and their reproducible analysis script are stored with the review under `workspace/Radcliffe-Yard-HPR-Next-Iteration-2026-09-07-v2/provenance/comparison-choices-20260907/`. Earlier review documents and checksums are preserved there before the status update.
